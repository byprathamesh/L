#include <stdio.h>

int main(){
    int a = 6;
    int b = 3;
    int c = a + b;
    printf("The value of a is %d and the value of b is %d and sum is %d",a,b,c);
    //Modulus operator is used to get the remainder
    printf("the reaminder when a is divided by b is: %d\n" , a%b);

    return 0;
}